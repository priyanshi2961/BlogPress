apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: blogpress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: blogpress-master2-user-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: SPRING_APPLICATION_NAME
          value: user-service
        - name: SPRING_CLOUD_CONFIG_ENABLED
          value: "false"
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://mysql:3306/user_service_db
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: http://eureka-server:8761/eureka
        - name: EUREKA_INSTANCE_HOSTNAME
          value: user-service
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "false"
        - name: SERVER_PORT
          value: "8081"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 90
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: blogpress
spec:
  selector:
    app: user-service
  ports:
  - port: 8081
    targetPort: 8081
    name: http
  type: ClusterIP

