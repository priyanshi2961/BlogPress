apiVersion: apps/v1
kind: Deployment
metadata:
  name: engagement-service
  namespace: blogpress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engagement-service
  template:
    metadata:
      labels:
        app: engagement-service
    spec:
      containers:
      - name: engagement-service
        image: blogpress-master2-engagement-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8083
          name: http
        env:
        - name: SPRING_APPLICATION_NAME
          value: engagement-service
        - name: SPRING_CLOUD_CONFIG_ENABLED
          value: "false"
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://mysql:3306/engagement_service_db
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: http://eureka-server:8761/eureka
        - name: EUREKA_INSTANCE_HOSTNAME
          value: engagement-service
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "false"
        - name: SERVER_PORT
          value: "8083"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 90
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: engagement-service
  namespace: blogpress
spec:
  selector:
    app: engagement-service
  ports:
  - port: 8083
    targetPort: 8083
    name: http
  type: ClusterIP

